import React from 'react'
import { CartContext } from '../contexts/CartProvider'

const CardCart = ({product}) => {
    const { aumentar, disminuir, removeFromCart } = useContext(CartContext)

    return (
        <>
            
        </>
    )
}

export default CardCart